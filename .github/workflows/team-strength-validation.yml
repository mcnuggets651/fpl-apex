name: Apex Team Strength Validation

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "config/apex.yaml"
      - "src/apex_fpl/models/fixtures.py"
      - "src/apex_fpl/models/team_goals.py"
      - "src/apex_fpl/data/understat.py"
      - "src/apex_fpl/data/core_insights.py"
      - "scripts/validate_team_strength.py"
      - "scripts/validate_team_strength_blend.py"
      - "scripts/validate_fixture_fallback_selection.py"
      - "tests/test_fixture_elo.py"
      - "docs/evidence/fixture_fallback_selection_*.json"
      - ".github/workflows/team-strength-validation.yml"

permissions:
  contents: read

concurrency:
  group: team-strength-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12.14"
      - name: Install frozen Apex test environment
        run: bash scripts/install_frozen_apex.sh
      - name: Static checks
        run: |
          ruff check \
            scripts/validate_team_strength.py \
            scripts/validate_team_strength_blend.py \
            scripts/validate_fixture_fallback_selection.py \
            src/apex_fpl/models/fixtures.py \
            src/apex_fpl/models/team_goals.py \
            src/apex_fpl/data/understat.py \
            src/apex_fpl/data/core_insights.py \
            tests/test_fixture_elo.py
      - name: Elo and fixture-surface regressions
        run: "$APEX_CORE_PYTHON" -m pytest -q tests/test_fixture_elo.py
      - name: Clean fallback selection evidence
        run: "$APEX_CORE_PYTHON" scripts/validate_fixture_fallback_selection.py --output reports/fixture_fallback_selection.json
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: always()
        with:
          name: team-strength-validation-${{ github.run_id }}
          path: reports/fixture_fallback_selection.json
          retention-days: 30
      - name: Enforce promotion evidence
        run: |
          "$APEX_CORE_PYTHON" - <<'PY'
          import json
          from pathlib import Path

          report = json.loads(Path('reports/fixture_fallback_selection.json').read_text())
          assert report['production_changed'] is False
          assert report['core_scope'] == 'Premier League only; cup rows explicitly excluded'
          assert report['understat_vs_neutral_replacement']['n_matches'] >= 700
          assert report['understat_vs_neutral_replacement']['pass'] is True
          assert report['epl_elo_understat_overlap_2025_26'] >= 300
          assert report['untouched_holdout']['n_matches'] >= 150
          print('Understat replacement gate passed on clean no-hindsight evidence; live hybrid remains conditional on complete EPL Elo.')
          PY

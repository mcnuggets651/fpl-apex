name: Champion Learning Proof Binding Land

on:
  push:
    branches: [v2/champion-authority-chain]

permissions:
  contents: write

concurrency:
  group: champion-learning-proof-bind-${{ github.ref }}
  cancel-in-progress: true

jobs:
  land:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
      - name: Apply exact learning proof binding
        run: python scripts/land_champion_learning_proof_binding.py
      - name: Validate diff
        run: git diff --check
      - name: Commit binding and retire landing files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm scripts/land_champion_learning_proof_binding.py
          rm .github/workflows/champion-learning-proof-bind-land.yml
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Bind production learning proofs to champion chain"
          git push origin HEAD:v2/champion-authority-chain

name: Archived — legacy Apex snapshot publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is archived and intentionally publishes no team."
          echo "Use the Apex Unified workflow / scripts/run_apex.py instead."

# Historical purpose: published data/generated/apex_latest.* from the old base pipeline.
# Superseded by the single canonical contract:
# data/generated/apex_recommendation_latest.*

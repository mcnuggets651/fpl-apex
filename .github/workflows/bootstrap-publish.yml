name: Archived — bootstrap Apex snapshot publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This one-shot bootstrap publisher is archived and intentionally publishes no team."
          echo "Use the Apex Unified workflow / scripts/run_apex.py instead."

# Historical purpose: seeded data/generated/apex_latest.* before the unified contract.
# Superseded by data/generated/apex_recommendation_latest.*.

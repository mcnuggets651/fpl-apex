name: Apex Unified Readiness Audit

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: apex-unified-readiness
  cancel-in-progress: true

jobs:
  readiness:
    uses: ./.github/workflows/adaptive-canonical-diagnostic.yml
    secrets: inherit

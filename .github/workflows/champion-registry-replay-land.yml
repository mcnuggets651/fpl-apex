name: Champion Registry Replay Land

on:
  push:
    branches: [v2/champion-authority-chain]

permissions:
  contents: write

concurrency:
  group: champion-registry-replay-land-${{ github.ref }}
  cancel-in-progress: true

jobs:
  land:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
      - name: Apply registry replay integration
        run: python scripts/land_champion_registry_replay.py
      - name: Validate diff
        run: git diff --check
      - name: Commit integration and retire landing files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm scripts/land_champion_registry_replay.py
          rm .github/workflows/champion-registry-replay-land.yml
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Bind champion authority to full registry replay"
          git push origin HEAD:v2/champion-authority-chain
